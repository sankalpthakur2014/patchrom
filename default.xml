<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <remote name="github"
           fetch=".." />

  <default revision="refs/heads/kitkat"
           remote="github"
           sync-j="4" />

  <project path="build" name="miui-v6/patchrom_build">
    <copyfile src="makefile" dest="Makefile" />
  </project>
  <project path="miui" name="miui-v6/patchrom_miui" />
</manifest>
